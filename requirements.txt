# Web application
fastapi==0.141.1
uvicorn[standard]==0.52.4
python-multipart==0.0.32
itsdangerous==2.2.0
jinja2==3.1.6
pydantic==2.13.4
starlette==1.6.0
psutil==7.2.2

# AI/ML runtime
faster-whisper==1.2.1
ctranslate2==4.8.1
onnxruntime==1.29.0
numpy==2.5.2
scipy==1.18.1
torch==2.13.0+cpu
torchaudio==2.11.0

# Translation
transformers==4.46.1
accelerate==1.14.0
sentencepiece==0.2.2
IndicTransToolkit==1.1.1

# Speech / TTS (Piper is the current default engine)
piper-tts==1.7.0

# Audio and media
soundfile==0.14.0
imageio-ffmpeg==0.6.0

# Build/package dependencies used by build.py
Pillow==12.3.0
PyInstaller==6.22.2

# The active environment uses the CPU-only torch wheel. If the configured
# package index cannot resolve this local-version pin, install with:
# python -m pip install -r requirements.txt --extra-index-url https://download.pytorch.org/whl/cpu

# Optional legacy Parler-TTS is intentionally excluded: the active app
# defaults to Piper, and the current Parler-TTS install was made with
# --no-deps from Git revision d108732cd57788ec86bc857d99a6cabd66663d68.
# Tests use unittest from the standard library; pytest is not installed.
