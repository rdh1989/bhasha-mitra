# Web application
fastapi==0.141.1
uvicorn[standard]==0.52.4
python-multipart==0.0.32
itsdangerous==2.2.0
jinja2==3.1.6
pydantic==2.13.4
starlette==1.6.0
psutil==7.2.2

# ASR and model runtime
faster-whisper==1.2.1
ctranslate2==4.8.1
onnxruntime==1.29.0
numpy==2.5.2
scipy==1.18.1
torch==2.13.0+cpu
torchaudio==2.11.0

# Translation
transformers==4.46.1
accelerate==1.14.0
sentencepiece==0.2.2
IndicTransToolkit==1.1.1

# Text-to-speech
# app.main imports both TTS engine modules; keep this exact installed Git
# revision even though Piper is the default engine.
parler-tts @ git+https://github.com/huggingface/parler-tts.git@d108732cd57788ec86bc857d99a6cabd66663d68
piper-tts==1.7.0

# Audio and media
soundfile==0.14.0
imageio-ffmpeg==0.6.0

# Build/package tool used by build.py. build.py also installs it on demand,
# but pinning it here makes a fresh development/build environment reproducible.
PyInstaller==6.22.2

# torch==2.13.0+cpu is the CPU-only wheel used by the current Windows .venv.
# Install this requirement with the PyTorch CPU wheel index when the default
# package index does not expose local-version CPU wheels:
# python -m pip install -r requirements.txt --extra-index-url https://download.pytorch.org/whl/cpu
